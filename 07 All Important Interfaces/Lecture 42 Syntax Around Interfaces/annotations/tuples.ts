//  We know that they are numbers, but that's pretty much it.
const carSpecs: [number, number] = [400, 3354];

//  in an object format right here because we are forced to put in a key, it is immediately obvious what we are working with.
const carStats = {
  horsePower: 400,
  weight: 3354,
};
